program exemplo;
var faca, queijo: real;

procedure x (a : integer);
var xa, xb: integer;
begin
	faca := xa + xb * x;
end;

procedure get (valor : integer);
var c, d, x1: integer;
begin
	faca := x + valor * 2;
end;

begin
	faca := faca + exemplo * 2;
end.