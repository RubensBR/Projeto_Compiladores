// ANDRE
{ Programa }
program exemplo ; {programa para teste de comentário}
  {teste do lexico}
var
   NUMERO, i, r  : integer;
   final   : integer;
   teste   : boolean;
   //teste de comentário
procedure soma(a: integer; b: integer);
var teste : integer;
begin
   teste := a + b;
end;

begin  {tente gerar um erro usando um caracter não permitido.. tipo $} 
   
   
   if (NUMERO >= 20) then
   begin
       while (NUMERO >= 20) do
	   begin
		  
	   end;
   end;
   final := NUMERO / 1;	  
end.